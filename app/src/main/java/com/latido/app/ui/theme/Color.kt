package com.latido.app.ui.theme

import androidx.compose.ui.graphics.Color

// Neutral, sober palette with a single accent. Green/amber/red are NOT here on purpose:
// those three are reserved exclusively for severity meaning (see Severity.kt).

// Accent (calm blue) — primary actions only.
val AccentLight = Color(0xFF2563EB)
val AccentDark = Color(0xFF7AA2FF)

// Light surfaces
val BackgroundLight = Color(0xFFF7F8FA)
val SurfaceLight = Color(0xFFFFFFFF)
val SurfaceVariantLight = Color(0xFFEDEFF3)
val OnBackgroundLight = Color(0xFF14181F)
val OnSurfaceVariantLight = Color(0xFF49505B)
val OutlineLight = Color(0xFFC4C9D2)

// Dark surfaces
val BackgroundDark = Color(0xFF0E1116)
val SurfaceDark = Color(0xFF161A21)
val SurfaceVariantDark = Color(0xFF222831)
val OnBackgroundDark = Color(0xFFEAEDF2)
val OnSurfaceVariantDark = Color(0xFFAEB6C2)
val OutlineDark = Color(0xFF3A424E)
